```python
print('Привет, мир')   # обычный вывод
a = 'Я люблю Пайтон ' # созадание переменной с определённым значением
print(a)               # вывод ЗНАЧЕНИЯ переменной
```

```python
b = input()             # после запуска будет ждать ввода, результат сохранится в переменную b
print(b)                # вывод того, что ввёл пользователь
c = input('Как тебя зовут? ')   # то, что в скобках принтуется (стоит добавлять пробел в конце, чтобы было удобнее печатать)
print(c)
```
