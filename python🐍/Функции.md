Встроенные функции: print(), input(), float(), list(), set(), range(), len()
## Синтаксис

```python
def func_name():                # defind
    print('I am an function')
    return 'good day!'
func_name()                    # вызов функции
```

```python
def func_name(input_object):
    tmp = str(input_object) + '1'   # локальная переменная
    return tmp
l = func_name('hello')
print(l)
```

