`scp -r ..\Резюме\ alex@84.201.162.228:/home/alex/`-перенос папки на сервер`
`ssh alex@84.201.162.228`- подключение к серверу
```bash 
# (при первичном подключении)
sudo apt update && sudo apt upgrade #обновить программы
sudo apt install python3-venv#установка программ для модулей пайтон
#при изменении кода
python3 -m venv venv#создание папки для модулей
source venv/bin/activate#активация окружения или (пайтон)
cd folder_name/#переход в папку с кодом
ls # показ всех папок
pip install -r req.txt#установка модулей из файла (req.txt - пример)
python -m uvicorn main:app --reload & #запуск сайта

```
