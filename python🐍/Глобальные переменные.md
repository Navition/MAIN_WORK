
```python
def multiplication(num):
    global c    # глобальная переменная
    num *= c    # локальная переменная
    return num
c = 2
b = multiplication(3)
print(b)
```

```python
 global page_url # для изменения глобальной переменной
 page_utl = 'new_url'
```
