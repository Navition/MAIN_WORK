```python
print("-".join()) - # собирает строку из элементов списка, используя в качестве разделителя строку, к которой применяется метод 
"words = ['Hello', 'Python']"
"print('-'.join(words))"
## Выводит
Hello-Python
```
```python
print(text.split()) # разбивает строку на слова, используя в качестве разделителя последовательность пробельных символов, и возвращает список из этих слов
print('ЗАГЛАВНЫЕ буквы'.find('A'))      # поиск индекса буквы "А" (поиск идёт слева направо и выводит индекс первой попавшейся буквы "А")
print('ЗАГЛАВНЫЕ буквы'.rfind('A'))     # поиск индекса буквы "А" (поиск идёт справа налево и выводит индекс первой попавшейся буквы "А")
print('ЗАГЛАВНЫЕ буквы'.lower())        # всё делает в нижнем регистре
print('ЗАГЛАВНЫЕ буквы'.upper())        # всё делает в верхнем регистре
print('Количество букв А', 'ЗАГЛАВНЫЕ буквы'.count('A'))
print("@", '  ЗАГЛАВНЫЕ буквы   '.strip(), "@")        #удаление пробелов
print('ЗАГЛАВНЫЕ буквы'.replace('буквы', 'символы'))    # замена
print()
print('t'.isalpha())                    # является строка буквой
print('1'.isdigit())                    # является ли строка цифрой
print(s.capitalize)                     #  возвращает копию строки , в которой первый символ имеет верхний регистр, а все остальные символы имеют нижний регистр.
print(s.swapcase())                     # возвращает копию строки , в которой все символы, имеющие верхний регистр, преобразуются в символы нижнего регистра и наоборот.
print(s.title())                        # возвращает копию строки , в которой первый символ каждого слова переводится в верхний регистр
print(s.count(<sub>, <start>, <end>))                        # считает количество **непересекающихся** вхождений подстроки
print(s.startswith(<prefix>, <start>, <end>)) #  определяет, **начинается** ли исходная строка `s` подстрокой `<prefix>`. Метод возвращает значение `True`, если исходная строка начинается с подстроки `<prefix>`, или `False` в противном случае
print(s.endswith(<suffix>, <start>, <end>)) # определяет, **оканчивается** ли исходная строка `s` подстрокой `<suffix>`. Метод возвращает значение `True`, если исходная строка оканчивается на подстроку `<suffix>`, или `False` в противном случае
print(s.strip())  # возвращает копию строки `s`, у которой удалены все пробелы, стоящие **в начале и конце** строки
print(s.isstrip()) # возвращает копию строки `s`, у которой удалены все пробелы, стоящие **в начале** строки
print(s.isalnum()) # определяет, состоит ли исходная строка из буквенно-цифровых символов. Метод возвращает значение `True`, если исходная строка является непустой и состоит **только** из буквенно-цифровых символов, или `False` в противном случае
print(s.islower()) # определяет, являются ли **все** буквенные символы исходной строки строчными (имеют нижний регистр). Метод возвращает значение `True`, если все буквенные символы исходной строки являются строчными, или `False` в противном случае
print(s.isupper()) # определяет, являются ли **все** буквенные символы исходной строки заглавными (имеют верхний регистр). Метод возвращает значение `True`, если все буквенные символы исходной строки являются заглавными, или `False` в противном случае
print(s.isspace()) # определяет, состоит ли исходная строка **только** из пробельных символов. Метод возвращает значение `True`, если строка состоит только из пробельных символов, или `False` в противном случае

```

## Split() - разбивает строку на слова, используя в качестве разделителя последовательность пробельных символов, и возвращает список из этих слов


```python
s = 'Python is the most powerful language'
words = s.split()
print(words)

### Выводит:
['Python', 'is', 'the', 'most', 'powerful', 'language']
```
![[Pasted image 20250525182057.png]]

## Join() - собирает строку из элементов списка, используя в качестве разделителя строку, к которой применяется метод

```python
words = ['Python', 'is', 'the', 'most', 'powerful', 'language']
s = ' '.join(words) 
print(s)

### Выводит:
Python is the most powerful language
```
![[Pasted image 20250525182247.png]]

