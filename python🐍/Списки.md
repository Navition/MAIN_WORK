```python
# пустой список
lst = []
lst = list() # ещё это функция трансформации в список

# в списках могут хранится разные типы данных
lst = ['str', 8, True, 7.5, [3, 4, 5]]
juice = ['яблоко', 'банан', 'апельсин', 'груша']
juice.append('виноград')    # добавление элемента
del juice[1]                # удаление по индексу
juice.remove('груша')       # удаление по значению элемента
juice.index('яблоко')       # нахождение индекса элемента
```

```python
# полезные функции
lst = [1, 2, 3, 4]
print('Сумма:', sum(lst))
print('Количество:', len(lst))
```

# Соединение списков

```python
list3 = list1 + list2
```

